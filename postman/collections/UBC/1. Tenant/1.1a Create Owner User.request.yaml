$kind: http-request
name: 1.1a Create Owner User
url: "{{base_url}}/users"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "email": "{{auth_email}}",
      "password": "password123",
      "full_name": "UBC Collection Owner"
    }
scripts:
  - type: beforeRequest
    code: |-
      const random = Math.random().toString(36).substring(2, 10);
      pm.collectionVariables.set('auth_email', `ubc_owner_${random}@ubc.xyz`);
    language: text/javascript
  - type: afterResponse
    code: |-
      const body = pm.response.json();
      pm.test('Owner user created successfully', () => {
          pm.expect(pm.response.code).to.equal(201);
          pm.expect(body.email).to.equal(pm.collectionVariables.get('auth_email'));
          pm.expect(body.is_deleted).to.equal(false);
      });
      pm.collectionVariables.set('auth_user_id', body.id);
    language: text/javascript
settings:
  followRedirects: true
  disableUrlEncoding: false
  disableCookies: false
order: 1100
