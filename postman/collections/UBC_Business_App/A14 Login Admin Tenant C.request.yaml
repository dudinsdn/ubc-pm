$kind: http-request
name: Login Admin Tenant C
url: "{{base_url}}/auth/login"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_c_id}}",
      "email": "{{admin_c_email}}",
      "password": "password123"
    }
scripts:
  - type: afterResponse
    code: |-
      const body = pm.response.json();
      pm.test('Login Admin Tenant C berhasil', () => {
          pm.expect(pm.response.code).to.equal(200);
          pm.expect(body.user_id).to.equal(pm.collectionVariables.get('admin_c_user_id'));
          pm.expect(body.tenant_id).to.equal(pm.collectionVariables.get('tenant_c_id'));
      });
      pm.collectionVariables.set('token_c', body.token);
    language: text/javascript
order: 4503595950000000
