$kind: http-request
name: Get Another User By ID Forbidden
url: "{{base_url}}/users/{{random_uuid_404}}"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('User lain tidak boleh dibaca', () => {
          pm.expect(pm.response.code).to.equal(403);
      });
    language: text/javascript
  - type: beforeRequest
    code: ""
    language: text/javascript
settings:
  followRedirects: true
  disableUrlEncoding: false
  disableCookies: false
order: 4412292529156102
