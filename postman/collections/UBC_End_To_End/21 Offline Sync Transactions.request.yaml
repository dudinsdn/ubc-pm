$kind: http-request
name: 21 Offline Incremental Sync Transactions
url: "{{base_url}}/businesses/{{business_id}}/transactions?updated_since={{offline_sync_cursor}}"
method: GET
queryParams:
  - key: updated_since
    value: "{{offline_sync_cursor}}"
    disabled: false
scripts:
  - type: afterResponse
    code: |-
      const body = pm.response.json();
      pm.test('Offline sync Transactions -> 200 and includes changed resource', () => {
          pm.expect(pm.response.code).to.equal(200);
          pm.expect(body.some(item => item.id === pm.collectionVariables.get('transaction_id'))).to.be.true;
      });
    language: text/javascript
settings:
  followRedirects: true
  disableUrlEncoding: false
  disableCookies: false
order: 21000
