$kind: http-request
name: Start ServiceOrder
url: "{{base_url}}/service-orders/{{service_order_id}}/start"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "expected_version": {{service_order_version}}
    }
scripts:
  - type: afterResponse
    code: |-
      const body = pm.response.json();
      pm.test('Start ServiceOrder -> 200 OK, status in_progress', () => {
          pm.expect(pm.response.code).to.equal(200);
      });
      pm.collectionVariables.set('service_order_version', body.version);
    language: text/javascript
settings:
  followRedirects: true
  disableUrlEncoding: false
  disableCookies: false
order: 4502479747556371
