$kind: http-request
name: L17 Delete Laundry Transaction
url: "{{base_url}}/transactions/{{laundry_transaction_id}}"
method: DELETE
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "expected_version": {{laundry_transaction_version}}
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Delete Laundry Transaction -> 204', () => {
          pm.expect(pm.response.code).to.equal(204);
      });
    language: text/javascript
settings:
  followRedirects: true
  disableUrlEncoding: false
  disableCookies: false
order: 33920
