$kind: http-request
name: W08A Platform Activate Tenant Lain
url: "{{base_url}}/platform/tenants/{{other_tenant_id}}/status"
method: PATCH
auth:
  type: bearer
  credentials:
    token: "{{platform_admin_token}}"
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "status": "active",
      "expected_version": {{other_tenant_version}}
    }
scripts:
  - type: afterResponse
    code: |-
      const body = pm.response.json();
      pm.test('Platform Admin mengaktifkan Tenant lain', () => {
          pm.expect(pm.response.code).to.equal(200);
          pm.expect(body.id).to.equal(pm.collectionVariables.get('other_tenant_id'));
          pm.expect(body.status).to.equal('active');
      });
      pm.collectionVariables.set('other_tenant_version', body.version);
    language: text/javascript
settings:
  followRedirects: true
  disableUrlEncoding: false
  disableCookies: false
order: 23465
