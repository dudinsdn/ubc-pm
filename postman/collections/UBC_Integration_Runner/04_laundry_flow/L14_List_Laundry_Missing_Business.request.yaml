$kind: http-request
name: L14 List Laundry Orders Missing Business -> 404
url: "{{base_url}}/businesses/{{uuid_404}}/laundry-orders"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('List Laundry for missing Business -> 404', () => {
          pm.expect(pm.response.code).to.equal(404);
      });
    language: text/javascript
settings:
  followRedirects: true
  disableUrlEncoding: false
  disableCookies: false
order: 23140
