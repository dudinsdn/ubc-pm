$kind: http-request
name: F01 Delete Other Transaction
url: "{{base_url}}/transactions/{{other_transaction_id}}"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{token_other_tenant}}"
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    { "expected_version": {{other_transaction_version}} }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Transaction fixture Tenant lain dihapus', () => {
          pm.expect(pm.response.code).to.equal(204);
      });
    language: text/javascript
order: 4504101000000000
