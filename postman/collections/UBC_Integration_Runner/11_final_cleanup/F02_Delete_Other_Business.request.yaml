$kind: http-request
name: F02 Delete Other Business
url: "{{base_url}}/businesses/{{other_business_id}}"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{token_other_tenant}}"
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    { "expected_version": {{other_business_version}} }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Business fixture Tenant lain dihapus', () => {
          pm.expect(pm.response.code).to.equal(204);
      });
    language: text/javascript
order: 4504102000000000
