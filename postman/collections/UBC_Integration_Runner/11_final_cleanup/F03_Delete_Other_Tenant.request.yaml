$kind: http-request
name: F03 Delete Other Tenant
url: "{{base_url}}/tenants/{{other_tenant_id}}"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{token_other_tenant}}"
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    { "expected_version": {{other_tenant_version}} }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Tenant lain dihapus setelah resource turunannya bersih', () => {
          pm.expect(pm.response.code).to.equal(204);
      });
    language: text/javascript
order: 4504103000000000
