$kind: http-request
name: F08 Cancel Subscription Tenant A
url: "{{base_url}}/platform/tenants/{{tenant_a_id}}/subscription"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{platform_access_token}}"
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    { "expected_version": {{tenant_a_subscription_version}} }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Subscription Tenant A dibatalkan', () => {
          pm.expect(pm.response.code).to.equal(200);
      });
    language: text/javascript
order: 4504108000000000
