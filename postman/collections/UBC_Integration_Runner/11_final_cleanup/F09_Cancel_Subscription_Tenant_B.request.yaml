$kind: http-request
name: F09 Cancel Subscription Tenant B
url: "{{base_url}}/platform/tenants/{{tenant_b_id}}/subscription"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{platform_access_token}}"
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    { "expected_version": {{tenant_b_subscription_version}} }
scripts:
  - type: afterResponse
    code: |-
      const body = pm.response.json();
      pm.test('Subscription Tenant B dibatalkan', () => {
          pm.expect(pm.response.code).to.equal(200);
          pm.expect(body.status).to.equal('cancelled');
      });
      pm.collectionVariables.set('tenant_b_subscription_version', body.version);
    language: text/javascript
order: 4504109000000000
