$kind: http-request
name: F12 Delete Tenant B
url: "{{base_url}}/tenants/{{tenant_b_id}}"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{token_b}}"
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    { "expected_version": {{tenant_b_version}} }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Tenant B dihapus', () => {
          pm.expect(pm.response.code).to.equal(204);
      });
    language: text/javascript
order: 4504112000000000
