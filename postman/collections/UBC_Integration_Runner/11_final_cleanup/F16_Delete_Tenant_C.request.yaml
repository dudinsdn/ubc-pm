$kind: http-request
name: F16 Delete Tenant C
url: "{{base_url}}/tenants/{{tenant_c_id}}"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{token_c}}"
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    { "expected_version": {{tenant_c_version}} }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Tenant C dihapus', () => {
          pm.expect(pm.response.code).to.equal(204);
      });
    language: text/javascript
order: 4504116000000000
